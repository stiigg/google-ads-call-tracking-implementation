# CallRail API Integration

Use the scripts in this directory to fetch call data or receive webhooks from CallRail.

## Files

- `fetch-calls.py`: Fetches calls via the CallRail REST API.
- `webhook-handler.js`: Minimal webhook listener for call events.
