# Google Ads Conversion Upload Cron Configuration
# Copy the relevant line(s) to your crontab using: crontab -e

# IMPORTANT: Update paths to match your installation directory
# Replace /path/to/google-ads-call-tracking-implementation with your actual path

# Set environment for cron
SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin

# ============================================================================
# RECOMMENDED: Upload conversions every 6 hours
# Runs at: 12am, 6am, 12pm, 6pm
# ============================================================================
0 */6 * * * cd /path/to/google-ads-call-tracking-implementation && ./code-templates/automation/scheduled-batch-upload.sh >> /var/log/google-ads-uploads.log 2>&1

# ============================================================================
# ALTERNATIVE: More frequent updates every 4 hours
# Runs at: 12am, 4am, 8am, 12pm, 4pm, 8pm
# ============================================================================
# 0 */4 * * * cd /path/to/google-ads-call-tracking-implementation && ./code-templates/automation/scheduled-batch-upload.sh >> /var/log/google-ads-uploads.log 2>&1

# ============================================================================
# ALTERNATIVE: Business hours only (9am-5pm, every 2 hours)
# Runs at: 9am, 11am, 1pm, 3pm, 5pm on weekdays
# ============================================================================
# 0 9-17/2 * * 1-5 cd /path/to/google-ads-call-tracking-implementation && ./code-templates/automation/scheduled-batch-upload.sh >> /var/log/google-ads-uploads.log 2>&1

# ============================================================================
# OPTIONAL: Daily error report via email
# Sends email if errors are found in the log file
# ============================================================================
# 0 9 * * * grep -i "error\|failed" /var/log/google-ads-uploads.log | mail -s "Google Ads Upload Errors" your-email@example.com

# ============================================================================
# OPTIONAL: Weekly log cleanup (keep last 30 days)
# Runs every Sunday at 3am
# ============================================================================
# 0 3 * * 0 find /path/to/google-ads-call-tracking-implementation/logs -name "*.log" -mtime +30 -delete

# ============================================================================
# Cron Schedule Format Reference:
# ┌───────────── minute (0 - 59)
# │ ┌───────────── hour (0 - 23)
# │ │ ┌───────────── day of month (1 - 31)
# │ │ │ ┌───────────── month (1 - 12)
# │ │ │ │ ┌───────────── day of week (0 - 6) (Sunday to Saturday)
# │ │ │ │ │
# * * * * * command to execute
# ============================================================================
